node_modules/
assets/icon.png
*.backup
.ai-agent-notifier-backup

# Local secrets — never commit
.env
.env.*
!.env.example

# Graphify knowledge-graph output — generated artifacts, never committed
# (note: .graphifyignore is intentional config and stays tracked)
graphify-out/
.graphify_*

# OS-generated cruft
Thumbs.db
.DS_Store
Desktop.ini
